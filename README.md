# Enterprise_Fintech_Platform
Enterprise-grade fintech backend built using Go with microservices architecture. Implements account, customer, payment, transaction, card, and balance services with gRPC and REST APIs. Follows clean architecture (handler → service → data → mapper), supports inter-service communication, and uses PostgreSQL, Docker, and Snowflake ID generation.
